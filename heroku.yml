build:
  docker:
    football_marketplace: ./Dockerfile
    web: ./football_marketplace-app/frontend/Dockerfile
run:
  football_marketplace: sh -c 'cd football_marketplace-app && npm install && node index.js'
  web: sh -c 'npm install && npm start'