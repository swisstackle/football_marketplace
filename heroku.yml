build:
  docker:
    web: ./Dockerfile
    #web: ./football_marketplace-app/frontend/Dockerfile
run:
  web: sh -c 'cd football_marketplace-app && npm install && node index.js'
  #web: sh -c 'npm install'