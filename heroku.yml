build:
  docker:
    web: ./Dockerfile
    #web: ./football_marketplace-app/frontend/Dockerfile
run:
  web: sh -c 'cd football_marketplace-app && npm install && cd frontend && npm install && cd ../ && node index.js'
  #web: sh -c 'npm install'