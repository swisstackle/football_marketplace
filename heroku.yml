build:
  docker:
    football_marketplace: ./Dockerfile
    reactjs: ./football_marketplace-app/frontend/Dockerfile
run:
  football_marketplace: npm install && npm start
  reactjs: npm install && npm start